/* The identity moved to components/brand/logo.tsx so the marketing site and the
   console draw the same mark from one file. Re-exported here because the nav,
   the footer and the chat mockups already import from this path. */
export { BrandLogo, BrandMark, BrandTile, BrandWordmark } from '@/components/brand/logo';
